<Window x:Class="DailyVitals.App.Views.FoodPhosphorusIntakeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Food Phosphorus Tracker"
        Height="780"
        MinHeight="720"
        Width="980"
        MinWidth="940"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style x:Key="MainMenuButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="220"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#707070"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D6D6D6"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C8C8C8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#EEEEEE"/>
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="150"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="95"/>
                <ColumnDefinition Width="275"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="270"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Person:" VerticalAlignment="Center"/>

            <ComboBox Grid.Column="1"
                      Width="260"
                      HorizontalAlignment="Left"
                      Margin="0,5"
                      ItemsSource="{Binding Persons}"
                      SelectedItem="{Binding SelectedPerson, Mode=TwoWay}"
                      DisplayMemberPath="FullName"/>

            <TextBlock Grid.Column="2"
                       Text="Search"
                       Margin="8,0,8,0"
                       VerticalAlignment="Center"/>

            <TextBox Grid.Column="3"
                     Height="30"
                     VerticalContentAlignment="Center"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Column="4"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"
                       FontWeight="SemiBold"
                       Text="{Binding SelectedDayTotalMg, StringFormat=Selected day total: {0} mg}"/>
        </Grid>

        <DataGrid Grid.Row="1"
                  Margin="0,10,0,0"
                  ItemsSource="{Binding History}"
                  SelectedItem="{Binding SelectedHistory}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HorizontalScrollBarVisibility="Auto"
                  SelectionMode="Single"
                  MouseDoubleClick="HistoryGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date"
                                    Width="110"
                                    Binding="{Binding ConsumedAt, StringFormat=d}"/>
                <DataGridTextColumn Header="Food Item"
                                    Width="2*"
                                    MinWidth="240"
                                    Binding="{Binding FoodName}"/>
                <DataGridTextColumn Header="Phosphorus (mg)"
                                    Width="125"
                                    Binding="{Binding PhosphorusMg}"/>
                <DataGridTextColumn Header="Calories"
                                    Width="80"
                                    Binding="{Binding Calories}"/>
                <DataGridTextColumn Header="Sodium (mg)"
                                    Width="95"
                                    Binding="{Binding SodiumMg}"/>
                <DataGridTextColumn Header="Protein (g)"
                                    Width="90"
                                    Binding="{Binding ProteinG, StringFormat={}{0:0.##}}"/>
                <DataGridTextColumn Header="Potassium (mg)"
                                    Width="105"
                                    Binding="{Binding PotassiumMg}"/>
                <DataGridTextColumn Header="Binders"
                                    Width="70"
                                    Binding="{Binding Binders}"/>
            </DataGrid.Columns>
        </DataGrid>

        <ScrollViewer Grid.Row="2"
                      Margin="0,12,0,0"
                      VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="170"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Date Eaten:"
                           VerticalAlignment="Center"/>
                <DatePicker Grid.Row="0"
                            Grid.Column="1"
                            Width="160"
                            HorizontalAlignment="Left"
                            Margin="0,4"
                            SelectedDate="{Binding ConsumedAt}"/>

                <TextBlock Grid.Row="1"
                           Text="Food Item:"
                           VerticalAlignment="Center"/>
                <TextBox Grid.Row="1"
                         Grid.Column="1"
                         Width="480"
                         HorizontalAlignment="Left"
                         Margin="0,4"
                         Text="{Binding FoodName, UpdateSourceTrigger=PropertyChanged}"/>

                <Button Grid.Row="1"
                        Grid.Column="1"
                        Width="120"
                        Height="32"
                        Margin="490,4,0,4"
                        HorizontalAlignment="Left"
                        Content="Estimate"
                        Style="{StaticResource MainMenuButtonStyle}"
                        Click="Estimate_Click"
                        IsEnabled="{Binding CanEstimate}"/>

                <TextBlock Grid.Row="2"
                           Text="Phosphorus (mg):"
                           VerticalAlignment="Center"/>
                <TextBox Grid.Row="2"
                         Grid.Column="1"
                         Width="120"
                         HorizontalAlignment="Left"
                         Margin="0,4"
                         Text="{Binding PhosphorusMg, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="3"
                           Text="Calories:"
                           VerticalAlignment="Center"/>
                <TextBox Grid.Row="3"
                         Grid.Column="1"
                         Width="120"
                         HorizontalAlignment="Left"
                         Margin="0,4"
                         Text="{Binding Calories, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="4"
                           Text="Sodium (mg):"
                           VerticalAlignment="Center"/>
                <TextBox Grid.Row="4"
                         Grid.Column="1"
                         Width="120"
                         HorizontalAlignment="Left"
                         Margin="0,4"
                         Text="{Binding SodiumMg, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="5"
                           Text="Protein (g):"
                           VerticalAlignment="Center"/>
                <TextBox Grid.Row="5"
                         Grid.Column="1"
                         Width="120"
                         HorizontalAlignment="Left"
                         Margin="0,4"
                         Text="{Binding ProteinG, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="6"
                           Text="Potassium (mg):"
                           VerticalAlignment="Center"/>
                <TextBox Grid.Row="6"
                         Grid.Column="1"
                         Width="120"
                         HorizontalAlignment="Left"
                         Margin="0,4"
                         Text="{Binding PotassiumMg, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="7"
                           Text="Binders:"
                           VerticalAlignment="Center"/>
                <TextBox Grid.Row="7"
                         Grid.Column="1"
                         Width="120"
                         HorizontalAlignment="Left"
                         Margin="0,4"
                         Text="{Binding Binders, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="8"
                           Text="Serving:"
                           VerticalAlignment="Center"/>
                <TextBox Grid.Row="8"
                         Grid.Column="1"
                         Width="300"
                         HorizontalAlignment="Left"
                         Margin="0,4"
                         Text="{Binding ServingDescription, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="9"
                           Text="AI Lookup:"
                           VerticalAlignment="Center"/>
                <StackPanel Grid.Row="9"
                            Grid.Column="1"
                            Orientation="Vertical"
                            Margin="0,4">
                    <TextBlock Text="{Binding EstimateStatus}"
                               FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding SourceNotes}"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <TextBlock Grid.Row="10"
                           Text="Food Notes:"
                           VerticalAlignment="Center"/>
                <Button Grid.Row="10"
                        Grid.Column="1"
                        Width="120"
                        Height="30"
                        Margin="0,4"
                        HorizontalAlignment="Left"
                        Content="Food Notes..."
                        Style="{StaticResource MainMenuButtonStyle}"
                        Click="FoodNotes_Click"
                        IsEnabled="{Binding CanEditFoodNotes}"/>

                <TextBlock Grid.Row="11"
                           Grid.Column="1"
                           Text="{Binding AuditTimestampText}"
                           Foreground="DimGray"
                           FontSize="12"
                           Margin="0,2,0,0"/>
            </Grid>
        </ScrollViewer>

        <Grid Grid.Row="3" Margin="0,8,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Width="100"
                    Height="32"
                    Content="New Entry"
                    Style="{StaticResource MainMenuButtonStyle}"
                    Click="NewEntry_Click"/>

            <StackPanel Grid.Column="2"
                        Orientation="Horizontal">
                <Button Width="125"
                        Height="32"
                        Margin="0,0,10,0"
                        Content="Running Totals"
                        Style="{StaticResource MainMenuButtonStyle}"
                        Click="RunningTotals_Click"
                        IsEnabled="{Binding CanViewRunningTotals}"/>

                <Button Width="100"
                        Height="32"
                        Margin="0,0,10,0"
                        Content="Delete"
                        Style="{StaticResource MainMenuButtonStyle}"
                        Click="Delete_Click"
                        IsEnabled="{Binding CanDelete}"/>

                <Button Width="100"
                        Height="32"
                        Margin="0,0,10,0"
                        Content="Save"
                        Style="{StaticResource MainMenuButtonStyle}"
                        Click="Save_Click"
                        IsEnabled="{Binding CanSave}"/>

                <Button Width="100"
                        Height="32"
                        Content="Cancel"
                        Style="{StaticResource MainMenuButtonStyle}"
                        Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
