<Window x:Class="DailyVitals.App.Views.ExerciseTrendView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Exercise Trend"
        Height="460"
        Width="720"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>

        <Style x:Key="MainMenuButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="220"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#707070"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="Black"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="#D6D6D6"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="#C8C8C8"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="#EEEEEE"/>
                                <Setter Property="Foreground"
                                        Value="#888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="{Binding PersonName}"
                       FontSize="18"
                       FontWeight="Bold"/>
            <TextBlock Text="Exercise Trend (minutes per session)"
                       FontSize="13"
                       Foreground="DimGray"/>
            <TextBlock Text="{Binding DateRange}"
                       FontSize="12"
                       Foreground="Gray"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Canvas Width="640"
                    Height="280"
                    Background="#FFF4F4F4"
                    HorizontalAlignment="Center">
                <Polyline Stroke="DarkOrange"
                          StrokeThickness="3"
                          Points="{Binding TrendPoints}"/>

                <TextBlock Text="{Binding StartMinutes, StringFormat=Start: {0:F0} min}"
                           Canvas.Left="5"
                           Canvas.Top="5"
                           FontSize="12"
                           Foreground="DimGray"/>

                <TextBlock Text="{Binding EndMinutes, StringFormat=End: {0:F0} min}"
                           Canvas.Right="5"
                           Canvas.Top="5"
                           FontSize="12"
                           Foreground="DimGray"/>
            </Canvas>
        </Grid>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding TrendSummary}"
                       FontSize="14"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Content="Close"
                    Width="90"
                    Height="32"
                    Style="{StaticResource MainMenuButtonStyle}"
                    Margin="10,0,0,0"
                    Click="Close_Click"/>
        </Grid>
    </Grid>
</Window>
