[build]
builder = "DOCKERFILE"

[deploy]
preDeployCommand = "dotnet DailyVitals.Web.dll --migrate-only"
healthcheckPath = "/health/ready"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
