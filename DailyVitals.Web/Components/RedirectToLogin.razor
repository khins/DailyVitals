@inject NavigationManager Navigation
@rendermode InteractiveServer

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
            Navigation.NavigateTo("/signin", forceLoad: true, replace: true);
    }
}
